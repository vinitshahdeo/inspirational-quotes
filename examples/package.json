{
	"name": "demo",
	"version": "1.0.1",
	"description": "demo of inspirational-quotes npm package",
	"main": "demo.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"test-lint": "eslint . --ext .js --ext .json, ./examples --ext .js --ext .json, ./data --ext .json --ignore-pattern node_modules/"
	},
	"keywords": [
		"inspiration",
		"motivation"
	],
	"author": "Vinit Shahdeo",
	"license": "MIT",
	"dependencies": {
		"inspirational-quotes": "^1.0.1"
	}
}
